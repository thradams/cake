// Test 2: auto storage-class specifier in an external declaration without type inference

auto int y; // Error: auto specifier cannot be used in an external declaration without type inference

int main() {
    return 0;
}
